apiVersion: v1
kind: PersistentVolume
metadata:
  name: toolbox-pv
  namespace: hysealab
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 5Gi
  persistentVolumeReclaimPolicy: Delete
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/git"
